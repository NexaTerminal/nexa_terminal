# Production API URL
REACT_APP_API_URL=https://nexa-terminal-api.onrender.com/api

# Disable ESLint plugin during production build
# This prevents ESLint warnings from failing the Vercel build
DISABLE_ESLINT_PLUGIN=true
